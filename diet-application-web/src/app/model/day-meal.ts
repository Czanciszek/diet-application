export interface DayMeal {
  id: string,
  header: object,
  primaryImageId: string,
  type: string,
  name: string,
  menuId: string,
  weekMealId: string,
  date: string,
  mealList: any[],
  dayType: string,
}
